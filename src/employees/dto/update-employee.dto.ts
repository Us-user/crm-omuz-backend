import { PartialType } from '@nestjs/swagger';

import { CreateEmployeeDto } from './create-employee.dto';

/**
 * Правка карточки сотрудника (ТЗ 5.14). Все поля необязательны: не переданное
 * остаётся прежним, пустая строка в необязательном поле очищает его.
 *
 * Телефон очистить нельзя — он обязателен и уникален; пустая строка в нём
 * отвергается как неразобранный номер (400), а не молча пропускается.
 *
 * `positionIds` ведут себя как галочки прав позиции (сессия 0006): не переданное
 * поле набор не трогает, пустой массив снимает все позиции.
 */
export class UpdateEmployeeDto extends PartialType(CreateEmployeeDto) {}
