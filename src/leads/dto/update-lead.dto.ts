import { PartialType } from '@nestjs/swagger';

import { CreateLeadDto } from './create-lead.dto';

/**
 * Правка лида (ТЗ 5.7). Не переданное поле остаётся прежним, пустая строка
 * очищает текст или снимает ссылку на курс, купон и филиал.
 *
 * Смена `type` — обычная правка карточки, но с побочным действием: переход
 * в `CLIENT` проставляет `becameClientAt`, а возврат в `LEAD` её снимает.
 * Отдельного маршрута «отметить клиентом» ТЗ не описывает, а два способа
 * сделать одно и то же разошлись бы (то же соображение, что с блокировкой
 * студента, 0015).
 */
export class UpdateLeadDto extends PartialType(CreateLeadDto) {}
