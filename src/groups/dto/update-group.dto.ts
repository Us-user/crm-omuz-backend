import { PartialType } from '@nestjs/swagger';

import { CreateGroupDto } from './create-group.dto';

/**
 * Правка группы. Не переданное поле остаётся прежним, пустая строка очищает
 * необязательное поле (описание, ссылку на чат, дату).
 *
 * Группу можно перенести в другой филиал и перевести на другой курс: и то и
 * другое — обычное исправление ошибки набора, а запрет заставил бы завести
 * группу заново и потерять всё, что на неё уже ссылается.
 */
export class UpdateGroupDto extends PartialType(CreateGroupDto) {}
