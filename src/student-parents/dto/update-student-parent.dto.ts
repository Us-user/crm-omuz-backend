import { PartialType } from '@nestjs/swagger';

import { CreateStudentParentDto } from './create-student-parent.dto';

/**
 * Правка родителя (ТЗ 4). Все поля необязательны: не переданное остаётся
 * прежним, пустая строка в необязательном поле очищает его.
 *
 * Телефон очистить нельзя — он ключ записи; пустая строка в нём отвергается
 * как неразобранный номер (400), а не молча пропускается. То же правило,
 * что у телефона студента (сессия 0014).
 *
 * Важно: правятся поля **общей** записи, поэтому изменения видны и в карточках
 * других детей этого родителя. Сколько их — говорит `childrenCount` в ответе.
 */
export class UpdateStudentParentDto extends PartialType(CreateStudentParentDto) {}
