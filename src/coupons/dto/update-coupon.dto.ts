import { PartialType } from '@nestjs/swagger';

import { CreateCouponDto } from './create-coupon.dto';

/**
 * Правка купона (ТЗ 5.7). Не переданное поле остаётся прежним, пустая строка
 * в необязательном текстовом поле или в дате очищает его.
 *
 * `courseIds` **заменяет набор целиком** — четвёртый раз то же решение, что
 * с галочками прав позиции (0006), «Show to group» (0009) и позициями
 * сотрудника (0020): экран сохраняет весь мультивыбор, и при слиянии снять
 * курс было бы нечем. Три состояния разведены явно: поле не передано — набор
 * не трогать, пустой список — «на все курсы», список — заменить.
 */
export class UpdateCouponDto extends PartialType(CreateCouponDto) {}
