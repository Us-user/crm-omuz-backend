-- Фаза 0: первая (пустая) миграция.
-- Доменных таблиц ещё нет; миграция нужна, чтобы Prisma создала историю миграций
-- (`_prisma_migrations`) и последующие миграции Фазы 1+ применялись поверх неё.

-- Расширение для генерации UUID на стороне БД (пригодится доменным моделям).
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
